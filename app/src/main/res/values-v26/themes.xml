<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <item name="queryBackground">@android:color/transparent</item> <!-- Fondo transparente -->
        <item name="android:background">@android:color/transparent
        </item> <!-- Elimina el subrayado -->

        <item name="android:textColor">@android:color/white</item> <!-- Color del texto -->
        <item name="android:textColorHint">@android:color/darker_gray</item> <!-- Color del hint -->
        <item name="android:iconTint">@android:color/white</item> <!-- Color de los íconos -->
    </style>
</resources>